@public
<Card title description>
  <style>
    .card {
      border: 1px solid rgba(0, 0, 0, .12);
      border-radius: 8px;
      padding: 16px;
    }
  </style>
  <article className="card">
    <h2>{title}</h2>
    <p>{description}</p>
    {children}
  </article>
</Card>
